<resources>
    <style name="Theme.APPIMV" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Hace la barra de estado transparente -->
        <item name="android:windowLightStatusBar">true</item> <!-- Cambia el color de los íconos para que sean visibles en fondo claro -->
    </style>
</resources>
